پرامپت‌های داده شده به مدل هوش برای پیاده‌سازی پروژه در این پرونده ثبت می‌شود.

## پرامپت ۱
بسیار خب، بیا اولین قدم پروژه PWiki رو برداریم. من نمی‌خوام کل ویکی رو یکجا بسازیم. می‌خوام با یک چیز خیلی ساده و کاربردی شروع کنیم.

**ویژگی اول:**
می‌خوام یک فایل `index.html` داشته باشم. وقتی این فایل رو توی مرورگر باز می‌کنم، باید به طور خودکار محتوای یک فایل به اسم `README.md` که کنارش هست رو بخونه و به صورت یک صفحه وب زیبا نمایش بده.

**نکات مهم برای این ویژگی:**
-   **بدون سرور:** باید مستقیماً با باز کردن فایل `index.html` کار کنه.
-   **راست‌چین:** چون برای فارسی هست، متن باید به صورت پیش‌فرض راست‌چین و خوانا باشه.
-   **ساده:** فعلاً به منوی کناری، لینک‌های دیگه، یا چیزهای پیچیده فکر نکن. فقط همین: باز کردن `index.html` و دیدن محتوای `README.md`.

یادت باشه که باید تمام قوانینی که در دستورالعمل اصلی بهت دادم رو رعایت کنی (جاوااسکریپت خالص، کد تمیز، ساختار فایل مشخص و غیره). من به عنوان مدیر محصول، منتظر دیدن این ویژگی ساده هستم تا بتونیم اولین نسخه رو تست کنیم.

## پرامپت ۲
ازت می‌خوام از هیچ فریم‌ورکی استفاده نکنی و پروژه را با جاوااسکریپت خام پیاده‌سازی کنی. فایل‌ها با پسوند tsx را از پروژه حذف کن.

## پرامپت ۳
ازت می‌خوام کارهای زیر را انجام بدی:
1. پروژه باید به صورت ماژولار پیاده‌سازی بشه (پوشه‌ای با نام src بساز)
2. کدهای css را از فایل HTML خارج کن و در پوشه styles قرار بده
3. از tailwind در پروژه استفاده نکن. فقط از css خام استفاده کن.
4. یادت باشه که کامنت‌های JSDoc باید به **زبان انگلیسی** باشن.
5. یک استایل ساده و تمیز برای نمایش متن مارک‌دان بنویس (مشابه خروجی قبلی ولی بدون Tailwind).
6. حتماً فونت وزیرمتن رو به صورت محلی تعریف کن (فعلاً مسیرش رو خالی بذار، بعداً اضافه می‌کنیم).
7. تمام استایل‌ها باید راست‌چین (RTL) باشن.

## پرامپت ۴
لطفاً کدهای جاوااسکریپتی که نوشتی رو **بازسازی (Refactor)** کن.

**انتظارات من از این بازسازی:**
-   **ماژولار کن:** کدها رو به بخش‌های کوچک‌تر و مستقل تقسیم کن. می‌خوام هر بخش فقط یک مسئولیت مشخص داشته باشه. مثلاً یک بخش مسئول راه‌اندازی کل برنامه باشه، یک بخش دیگه فقط مسئول نمایش محتوا و... .
-   **از ساختار پوشه‌ای که تعریف کردیم استفاده کن:** لطفاً این بخش‌های مختلف رو در فایل‌های جداگانه داخل پوشه `src/core/` قرار بده.
     - یک بخش فقط مسئول خوندن فایل باشه
     - یک بخش فقط مسئول تبدیل markdown به HTML باشه
     - یک بخش فقط مسئول نمایش محتوا روی صفحه باشه
-   **یک "مغز متفکر" بساز:** می‌خوام یک کلاس یا ماژول اصلی به نام `App` وجود داشته باشه که مثل مغز برنامه عمل کنه و مسئولیت هماهنگی بین بخش‌های مختلف رو به عهده بگیره.
-   **فایل ورودی ساده باشه:** فایل `src/index.js` باید خیلی ساده باشه و فقط وظیفه‌اش این باشه که این "مغز متفکر" رو روشن کنه و بگه "شروع کن!".

**نتیجه نهایی برای من نباید تغییری کنه:**
بعد از این بازسازی، وقتی من `index.html` رو باز می‌کنم، هنوز باید همون صفحه قبلی رو ببینم. این تغییر فقط یک بهینه‌سازی داخلی برای آینده پروژه است.

## پرامپت ۵
می‌خوام یک **منوی کناری (Sidebar)** به صفحه اضافه کنی تا کاربر بتونه بین صفحه‌های مختلف ویکی جابجا بشه.

**انتظارات من از این ویژگی:**

1. **محتوای منو از یک فایل خونده بشه:** 
   منو باید محتواش رو از یک فایل به اسم `sidebar.md` که در پوشه `config/` هست بخونه. 
   
   فرمت این فایل یک لیست مارک‌دان ساده:
   ```markdown
   - [صفحه اصلی](/)
   - [راهنمای نصب](/guide)
   - [درباره ما](/about)
   ```

2. **قابلیت جابجایی (Navigation):**
   - وقتی من روی هر لینک کلیک می‌کنم (مثلاً "راهنمای نصب")، محتوای `guide.md` باید نمایش داده بشه
   - آدرس مرورگر هم تغییر کنه (مثلاً به `#/guide`)
   - کاربر بتونه لینک رو کپی و اشتراک‌گذاری کنه
   - دکمه Back و Forward مرورگر کار کنن

3. **ساختار فایل‌ها:**
   فایل‌های markdown می‌تونن در هر جایی باشن. مثلاً:
   - `README.md` برای صفحه اصلی (/)
   - `guide.md` برای راهنما (/guide)  
   - `about.md` برای درباره ما (/about)
   - یا حتی در پوشه‌ها: `docs/intro.md` برای (/docs/intro)

**نکات مهم:**
- **مکان منو:** در سمت **راست** صفحه (چون فارسی است)
- **معماری:** به صورت ماژولار پیاده‌سازی بشه
- **ظاهر:** ساده و خوانا باشه، با فونت فارسی مناسب
- **مشخص شدن صفحه فعال (Active State):** وقتی من در یک صفحه هستم (مثلاً صفحه "درباره ما")، لینک مربوط به اون در منوی کناری باید به صورت "فعال" یا "هایلایت" شده نمایش داده بشه تا کاربر بدونه کجاست.
- **رفتار در موبایل:** در صفحه‌های کوچک (موبایل)، منوی کناری باید به صورت پیش‌فرض مخفی باشه و با کلیک روی یک دکمه (مثلاً آیکون همبرگری ☰) از کنار صفحه باز و بسته بشه.
- **قابلیت جمع‌شوندگی:** منوی کناری قابلیت جمع‌شوندگی داشته باشه.

## پرامپت ۶
می‌خوام منوی کناری از **ساختار تودرتو (Nested Menu)** پشتیبانی کنه.

**انتظارات من از این ویژگی:**
1.  **پشتیبانی از لیست‌های تودرتو در مارک‌دان:**
    منو باید بتونه ساختاری مثل این رو در فایل `sidebar.md` به درستی نمایش بده:

```markdown
- [صفحه اصلی](/)
- [راهنمای نصب](/guide)
- [درباره ما](/about)
- سندها
    - [مقدمه](/docs/intro)
    - [آغاز](/docs/start)
- [منابع](/resources/home)
    - [مقالات](/resources/articles)
    - [ویدیوها](/resources/videos)
```
در این مثال، "بخش سندها" یک عنوانه و لینک‌های "مقدمه" و "شروع به کار" زیرمجموعه اون هستن.

2.  **ظاهر و رفتار منوی تودرتو:**
    -   زیرمنوها باید به صورت پیش‌فرض **جمع‌شده (Collapsible)** باشن.
    -   وقتی من روی عنوان والد (مثل "بخش سندها") کلیک می‌کنم، زیرمنوی اون باید با یک انیمیشن ساده باز بشه. با کلیک دوباره، باید بسته بشه.
    - وقتی روی عنوان والدی مثل «منابع» کلیک می‌کنم باید محتوای اون و زیر منو نمایش داده بشه.
   - آیتم‌های زیرمجموعه (مثل "مقدمه" و "آغاز") باید با تورفتگی مناسب نمایش داده بشن.

3.  **حفظ حالت فعال (Active State):**
    -   وقتی من روی یک لینک زیرمجموعه (مثلاً "مقدمه") کلیک می‌کنم، اون لینک باید هایلایت بشه.
    -   **مهم:** والد اون لینک ("بخش سندها") هم باید در حالت "باز" باقی بمونه تا کاربر ساختار رو گم نکنه.

**نکات مهم معماری و پیاده‌سازی:**
-   این تغییر احتمالاً روی ماژول `sidebar.js` و فایل CSS (`main.css`) تاثیر می‌ذاره.
-   منطق پارس کردن مارک‌دان در `sidebar.js` باید هوشمندتر بشه تا لیست‌های تودرتو رو تشخیص بده و به ساختار HTML درستی تبدیل کنه.
-   باید کدهای جاوااسکریپت لازم برای باز و بسته کردن منوها و همچنین استایل‌های CSS جدید برای نمایش حالت‌های مختلف (باز، بسته، فعال) رو اضافه کنی.

**یادآوری:**
این قابلیت برای سازماندهی مستندات پیچیده حیاتیه. لطفاً مطمئن شو که هم از نظر ظاهری زیبا و کاربرپسند باشه و هم از نظر فنی تمیز پیاده‌سازی بشه.

## پرامپت ۷
در پنل کناری نویسه '‹' در سمت چپ قرار گرفته در حالیکه باید سمت راست متن باشه. دقت کن این پروژه باید با زبان فارسی سازگار باشه.

## پرامپت ۸
در پنل کناری تغییرات زیر را اعمال کن:
1. زیر منوها نسبت به منو اصلی تو رفتگی داشته باشه. (دقت کن سازگار با زبان فارسی باشه)
2. برای جمع‌شوندگی از آیکون همبرگری استفاده کن.
3. زمانی که نوار کنار جمع می‌شه باید به صورت کامل جمع بشه و چیزی دیده نشه غیر از آیکون همبرگری

## پرامپت ۹
متاسفانه تورفتگی زیرمنوها انجام نشده، دقت کن باید به سمت چپ تورفتگی باشه. و اینکه منوی همبرگری ثباته، که باعث شده در حالتیکی منو کناری بازه روی نام ویکی قرار بگیره.

## پرامپت ۱۰
می‌خوام ساختار فایل‌های استایل رو بهبود بدی تا پروژه تمیزتر و قابل نگهداری‌تر بشه.

1. ساختار پیشنهادی CSS:

```
src/styles/
├── main.css              # فایل اصلی که همه رو import می‌کنه
├── variables.css          # فقط CSS Variables
├── base.css               # Reset, Typography, Base styles
├── layout.css             # Layout اصلی (app-container, grid)
├── components/
│   ├── sidebar.css        # استایل sidebar
│   ├── content.css        # استایل بخش محتوا
│   ├── prose.css          # استایل markdown
│   └── messages.css       # loading, error messages
└── utilities/
    └── responsive.css     # Media queries
```

2. **بهینه‌سازی کد:**
   - کدهای تکراری رو حذف کن
   - از بهترین روش‌های CSS استفاده کن
   - کامنت‌های توضیحی اضافه کن (به فارسی برای راحتی کاربران ایرانی)

## پرامپت ۱۱
در بخش css تغییرات زیر را انجام بده

1. **جهت صفحه (RTL):**
   در فایل `base.css`، به تگ `html` خاصیت `dir="rtl"` رو اضافه کن:
   ```css
   html {
     direction: rtl;
   }
   ```
   و از `layout.css` خط `direction: rtl;` رو از `.app-container` حذف کن چون اونجا کاری نداره.


2. **sidebar در موبایل:**
   در فایل `layout.css`، استایل پیش‌فرض sidebar برای موبایل رو اضافه کن:
   ```css
   /* در موبایل، sidebar پیش‌فرض بسته است */
   @media (max-width: 767px) {
     #sidebar {
       transform: translateX(100%); /* بیرون از صفحه */
     }
     
     body:not(.sidebar-collapsed) #sidebar {
       transform: translateX(0); /* باز کردن sidebar */
     }
   }
   ```

3. **ارتفاع submenu:**
   در `components/sidebar.css`، به جای `max-height: 50vh`، از یک مقدار خیلی بزرگ استفاده کن تا همیشه کامل نمایش داده بشه:
   ```css
   .has-submenu.open > .submenu {
     max-height: 1000px; /* یا هر مقدار بزرگی */
   }
   ```

## پرامپت ۱۲
تغییرات زیر را اعمال کن:

1. **تناقض در رفتار sidebar:**
   الان رفتار sidebar در موبایل و دسکتاپ با هم تناقض داره و گیج‌کننده است. باید یک منطق ساده و واضح داشته باشیم:
   - **دسکتاپ:** sidebar به طور پیش‌فرض باز است. با کلیک روی دکمه جمع می‌شه.
   - **موبایل:** sidebar به طور پیش‌فرض بسته است. با کلیک روی دکمه hamburger باز می‌شه.

2. **منطق کلاس `sidebar-collapsed`:**
   این کلاس فقط برای **جمع کردن sidebar** استفاده بشه (در دسکتاپ و موبایل یکسان).
   - وقتی `sidebar-collapsed` هست → sidebar جمع/بسته است
   - وقتی `sidebar-collapsed` نیست → sidebar باز است

3. **تورفتگی زیرمنوها:**
   زیرمنوها باید نسبت به منوی اصلی تورفتگی داشته باشن. چون زبان فارسی راست به چپ است، تورفتگی باید به سمت **چپ** باشه (یعنی `padding-right` بیشتر).

4.  **استفاده از `class` به جای `id`:** لطفاً در تمام فایل‌های CSS، به جای استفاده از سلکتورهای `id` (مثل `#sidebar` و `#content`)، از سلکتورهای `class` (مثل `.sidebar` و `.content`) استفاده کن. فایل `index.html` رو هم متناسب با این تغییر، آپدیت کن. این کار باعث می‌شه کدهامون در آینده قابل استفاده مجدد باشن.

**نتیجه نهایی:**
بعد از این تغییرات، ما باید یک منوی کناری داشته باشیم که:
-   در همه دستگاه‌ها به طور پیش‌فرض بسته است.
-   با یک آیکون همبرگری باز و بسته می‌شه.
-   زیرمنوهایش تورفتگی دارن.
-   و کد CSS اون تمیزتر و حرفه‌ای‌تر شده.

## پرامپت ۱۳
زیرمنوها در پنل کناری همچنان در راستای منو اصلی هست. کد زیر در بخش console غیرفعاله:

```css
.submenu a {
  padding-right: 4.5rem;
}
```

## پرامپت ۱۴
تغییرات زیر را اعمال کن:
1. در حالت desktop منوی کناری به صورت پیش‌فرض باز باشه.
2. زمانی که منو بسته می‌شه بخش محتوا به سمت راست صفحه می‌ره و جای منوی کناری قرار می‌گیره، می‌خوام با بسته شدن منوی کنار اندازه نمایش محتوا بیشتر بشه نه اینکه جای منوی کناری رو بگیره.
3. آیکون همبرگری حس جداافتادگی از منو کناری می‌ده، می‌خوام به صورت یکپارچه دیده بشه.

## پرامپت ۱۵
من بهت نگفتم جای آیکون همبرگری را تغییر بدی، فقط از خواستم که با منو کناری یکپارچه بشه با تغییری که ایجاد کردی آیکون روی نام ویکی افتاده.

## پرامپت ۱۶
زمانی که منو بسته می‌شه بخش محتوا به سمت راست صفحه می‌ره و جای منوی کناری قرار می‌گیره، می‌خوام با بسته شدن منوی کنار اندازه نمایش محتوا بیشتر بشه نه اینکه جای منوی کناری رو بگیره.

## پرامپت ۱۷
می‌خوام در زمان ورود به ویکی به صورت پیش‌فرض نخستین صفحه که ریشه (/) است نمایش داده بشه.

## پرامپت ۱۸
ازت می‌خوام مشکل‌های زیر را بر طرف کنی:
1.  **دکمه منو در موبایل نمایش داده نمی‌شه:** الان در حالت موبایل، دکمه‌ای برای باز و بسته کردن منو وجود نداره و کاربر نمی‌تونه به منو دسترسی داشته باشه.
2.  **عدم پشتیبانی از منوی چند سطحی:** اگه بخوام یک منوی تودرتوی چند سطحی (مثلاً ۳ سطح یا بیشتر) بسازم، فقط سطح اول زیرمنو تورفتگی داره و بقیه سطوح به درستی نمایش داده نمی‌شن.

```markdown
- منو اصلی
  - زیرمنو سطح ۱
    - زیرمنو سطح ۲
      - زیرمنو سطح ۳
  - زیرمنو دیگه سطح ۱
```
- تورفتگی هر سطح باید **بیشتر** از سطح قبلی باشه

3.  **یک مشکل فنی کوچک:** در کد از `#sidebar-nav` به عنوان ID استفاده شده که بهتره به صورت Class (`.sidebar-nav`) باشه تا کد ما استانداردتر و قابل استفاده مجدد بشه.

## پرامپت ۱۹
تغییراتی که ایجاد کردی باعث شده زیرمنوها هیچ تورفتگی نداشته باشن.

## پرامپت ۲۰
**مشکل فعلی:**
در حال حاضر، تمام تنظیمات پروژه (مثل مسیر فایل منو یا نام صفحه اصلی) مستقیماً داخل کدهای جاوااسکریپت نوشته شده. این یعنی اگه یک کاربر بخواد این مسیرها رو عوض کنه، مجبوره کد رو باز کنه و تغییر بده که خیلی سخت و ترسناکه.

**راه‌حل:**
می‌خوام تمام این تنظیمات از یک **فایل تنظیمات مرکزی** به نام `config.js` که در پوشه `config` قرار داره، خونده بشن. این فایل مثل یک "پنل کنترل" برای کاربر عمل می‌کنه.

**انتظارات من:**
1.  **یک فایل `config.js` جدید بساز:** این فایل رو در پوشه `config` ایجاد کن.
2.  **تمام تنظیمات رو به این فایل منتقل کن:** تمام مواردی که الان در `src/index.js` هستن باید به این فایل جدید منتقل بشن.
    - تنظیمات ظاهری (مثل رنگ اصلی، فونت)
    - مسیر فایل‌ها
    - شناسه‌های المنت‌ها
    - تنظیمات رفتاری (مثل فعال/غیرفعال بودن sidebar)
3.  **برنامه باید از این فایل بخونه:** کد اصلی برنامه باید تنظیماتش رو از این فایل جدید برداره، نه از مقادیر هاردکد شده.
4.  **توضیحات خوب برای کاربر:** لطفاً داخل فایل `config.js` کامنت‌های واضح و به زبان فارسی بنویس تا یک کاربر غیرفنی به راحتی بفهمه هر تنظیم برای چیه و چطور می‌تونه تغییرش بده.

**یک نکته خیلی مهم:**
اگه کاربر این فایل `config.js` رو نساخت یا فایل خالی بود، برنامه نباید خراب بشه! در این حالت، برنامه باید از یک سری **مقادیر پیش‌فرض منطقی** استفاده کنه تا باز هم کار کنه.

**یادآوری معماری:**
یادت باشه که این تغییر باید با معماری ماژولار فعلی ما هماهنگ باشه. احتمالاً به یک ماژول جدید برای مدیریت تنظیمات در پوشه `src/core/` نیاز خواهیم داشت.

**خلاصه وظیفه:**
هدف اصلی اینه که کاربر بتونه تمام تنظیمات پروژه رو به راحتی از یک فایل `config.js` تغییر بده، بدون اینکه نیازی به دستکاری کدهای اصلی داشته باشه.

## پرامپت ۲۱
بخش پیکربندی خیلی خوب کار می‌کنه. حالا می‌خوام دو تا بهبود کوچیک ولی مهم روش اعمال کنیم:

**بهبود اول: مشکل فایل تنظیمات اجباری**

الان اگه کاربر فایل `config/config.js` رو نداشته باشه یا اشتباهاً پاکش کنه، برنامه خطا می‌ده و کار نمی‌کنه. این برای کاربر ترسناکه!

می‌خوام طوری بشه که:
- اگه فایل `config.js` وجود داشت، از اون استفاده کنه
- اگه فایل وجود نداشت یا خطا داشت، برنامه **بدون مشکل** با تنظیمات پیش‌فرض کار کنه

**راهنمایی فنی:** احتمالاً نیاز هست که به جای استفاده از تگ `<script>` در HTML، از `import` داینامیک در جاوااسکریپت استفاده کنی و با `try-catch` خطاها رو مدیریت کنی.

**بهبود دوم: امکان فعال/غیرفعال کردن منوی کناری**

الان کاربر می‌تونه رنگ‌ها و مسیر فایل‌ها رو تغییر بده، ولی نمی‌تونه رفتار منوی کناری رو کنترل کنه.

می‌خوام در فایل `config.js` این تنظیمات رو هم اضافه کنی:

```javascript
sidebar: {
  enabled: true,           // آیا منوی کناری اصلاً نمایش داده بشه؟
}
```

**نکته مهم:**
مطمئن شو که این تنظیمات رو به `DEFAULT_CONFIG` هم اضافه کنی تا اگه کاربر چیزی ننوشت، مقادیر منطقی داشته باشیم.

## پرامپت ۲۲
می‌خوام سندهای کامل و حرفه‌ای برای پروژه بنویسی.

**فایل‌های مورد نیاز:**

**1. README.md (در ریشه پروژه)**
- معرفی کوتاه PWiki (چیه و چه مشکلی حل می‌کنه)
- ویژگی‌های کلیدی (Bullet points)
- نصب سریع در 3-4 خط
- یک مثال ساده
- لینک به سندهای کامل
- لینک به لایسنس

**2. docs/gettingStarted.md**
- پیش‌نیازها (فقط مرورگر!)
- دانلود و نصب (گام به گام)
- ساختار اولیه فایل‌ها
- اجرای اولین ویکی
- نحوه اجرا با سرور محلی (چرا لازمه؟)
- حل مشکل CORS

**3. docs/configuration.md**
- توضیح کامل فایل `config/config.js`
- هر بخش با مثال:
  - basePath
  - files (sidebar, defaultPage)
  - selectors
  - sidebar (enabled)
  - theme
- نکات مهم و هشدارها
- مثال‌های کاربردی (مثلاً تغییر رنگ، غیرفعال کردن sidebar)

**4. docs/writingContent.md**
- نحوه نوشتن فایل‌های Markdown
- ساختار دلخواه فایل‌ها (بدون محدودیت)
- ساخت منوی کناری (`sidebar.md`) با مثال
- لینک‌های داخلی
- اضافه کردن تصاویر
- نکات بهینه‌سازی محتوا

**5. docs/architecture.md**
- معماری کلی پروژه
- ساختار پوشه‌ها و فایل‌ها
- نقش هر ماژول اصلی:
  - core/ (app, router, sidebar, configManager, ...)
  - parsers/
  - styles/
- جریان اجرای برنامه (از ابتدا تا رندر صفحه)
- سیستم Event و Plugin
- نمودار ساده معماری (متنی)

**6. LICENSE**
- MIT License استاندارد
- نام پروژه: PWiki
- سال: 2025

**نکات مهم:**
- تمام مستندات باید به **زبان فارسی** باشن (به جز LICENSE)
- لحن ساده و قابل فهم برای کاربر غیرفنی
- مثال‌های عملی و واقعی
- ساختار تمیز با استفاده از heading ها
- کدهای نمونه با syntax highlighting مناسب

## پرامپت ۲۳
وقتی کاربر یک آدرس اشتباه وارد می‌کنه یا فایلی وجود نداره، فقط یک پیام خطای ساده نمایش داده می‌شه که کاربرپسند نیست.

می‌خوام یک **صفحه 404 اختصاصی** به پروژه اضافه کنی.

1. **فایل 404 جدید:** یک فایل به نام `404.md` در پوشه `config` پروژه بساز که محتوای دوستانه و کاربرپسندی داشته باشه. این محتوا باید شامل:
   - یک پیام محترمانه به فارسی (مثل "صفحه مورد نظر پیدا نشد")
   - توضیح کوتاه که ممکنه چه اتفاقی افتاده
   - لینک بازگشت به صفحه اصلی
   - شاید یک شوخی کوچک یا جمله دوستانه

2. **نمایش خودکار:** وقتی یک فایل پیدا نمی‌شه، به جای نمایش پیام خطای ساده فعلی، محتوای فایل `404.md` رو به کاربر نشون بده.

3. **قابل سفارشی‌سازی:** کاربر باید بتونه این فایل `404.md` رو ویرایش کنه و پیام خودش رو بنویسه.

4. **مسیر قابل تنظیم:** مسیر فایل 404 باید در `config.js` قابل تنظیم باشه. مثلاً کاربر بتونه بگه فایل 404 من در `pages/not-found.md` هست.

**نکته مهم:** اگه فایل `404.md` هم وجود نداشته باشه، همون پیام خطای ساده فعلی به فارسی نمایش داده بشه (فالبک).

## پرامپت ۲۴
نام پروژه به **شیرازه (Shirazeh)** تغییر کرده، می‌خوام در تمام پروژه، نام **PWiki** به **شیرازه (Shirazeh)** تغییر پیدا کنه:

**تغییرات مورد نیاز:**

1. **کد JavaScript:**
   - `window.PWIKI_USER_CONFIG` → `window.shirazeh`
   - تمام کامنت‌ها و پیام‌های console
   - JSDoc ها

2. **فایل HTML:**
   - `<title>PWiki</title>` → `<title>شیرازه</title>`
   - نام در sidebar header

3. **فایل config:**
   - نام متغیر
   - کامنت‌های فارسی

4. **نام‌های فایل (اگه لازمه):**
   - اگه فایلی با نام pwiki وجود داره

**نکات مهم:**
- در متن‌های فارسی از "شیرازه" استفاده کن
- در کدها و نام‌های متغیر از "Shirazeh" یا "shirazeh" استفاده کن
- حتماً consistency (یکنواختی) رو رعایت کن
- اسم انگلیسی رو با حروف کوچک بنویس: `shirazeh` (نه `Shirazeh` یا `SHIRAZEH`)

## پرامپت ۲۵
الان ساختار HTML برنامه (sidebar، content و دکمه toggle) مستقیماً در فایل `index.html` نوشته شده. می‌خوام این ساختار HTML از `index.html` حذف بشه و به صورت **پویا توسط JavaScript** ساخته بشه.

1. **ساده‌سازی index.html:** فایل `index.html` فقط باید یک `<div id="app"></div>` خالی داشته باشه. همه چیز دیگه باید با JavaScript ساخته بشه.
2. **ساخت ساختار در کد:** یک متد بساز که ساختار اصلی رو بسازه:
   - اگه sidebar فعال باشه sidebar و toggle button رو بساز
   - اگه غیرفعال باشه فقط بخش content
   - نام برنامه در sidebar header از config خونده بشه

3. **حفظ selectors:** کلاس‌های CSS (`.sidebar`, `.content`, ...) همونطور که هستن بمونن تا استایل‌ها خراب نشن.
4. **ترتیب اجرا:** مطمئن شو که ساختار HTML قبل از اینکه بقیه کامپوننت‌ها بخوان از اون استفاده کنن، ساخته بشه.

## پرامپت ۲۶
با توجه به تغییراتی که دادی سندها را بروزرسانی کن.

## پرامپت ۲۷
در حال حاضر، شیرازه فقط یک تم تاریک داره و کاربر هیچ کنترلی روی اون نداره. می‌خوام یک **تم روشن** به پروژه اضافه کنی و به کاربر اجازه بدم بین تم روشن و تاریک جابجا بشه.

1.  **یک تم روشن زیبا:**
    - لطفاً یک تم روشن، تمیز و مدرن برای شیرازه طراحی کن.
    - رنگ‌بندی باید خوانا و آرامش‌بخش باشه.
    - تمام بخش‌ها (منوی کناری، محتوا، کدها و...) باید در تم روشن هم ظاهر خوبی داشته باشن.
    - با تم تاریک فعلی هماهنگ باشه (همون رنگ‌های اصلی ولی معکوس)

2.  **تشخیص خودکار تم سیستم:** وقتی کاربر برای نخستین بار وارد سایت می‌شه، شیرازه باید به طور خودکار تشخیص بده که تم سیستم‌عامل کاربر (ویندوز، مک، اندروید و...) روشن هست یا تاریک، و همون تم رو به عنوان پیش‌فرض اعمال کنه.

3.  **دکمه جابجایی تم:** یک دکمه ساده (مثلاً با آیکون خورشید ☀️ و ماه 🌙) در یک جای مناسب اضافه کن.
    -   وقتی کاربر روی این دکمه کلیک می‌کنه، تم بین حالت روشن و تاریک جابجا بشه.
    -   این دکمه باید با توجه به تم فعال، آیکون مناسب رو نشون بده (مثلاً در تم روشن، آیکون ماه نمایش داده بشه).

4.  **ذخیره انتخاب کاربر:** وقتی کاربر یک تم رو انتخاب می‌کنه، این انتخاب باید در حافظه مرورگر ذخیره بشه. دفعه بعدی که همون کاربر وارد سایت می‌شه، آخرین تمی که انتخاب کرده بود باید نمایش داده بشه (نه تم سیستم‌عاملش).

5.  **قابل پیکربندی در config:** در فایل `config.js` باید بشه تم پیش‌فرض رو مشخص کرد:
```js
theme: {
  default: 'auto',  // 'light', 'dark', یا 'auto' (تشخیص از سیستم)
  // رنگ‌های سفارشی...
}
```

**نکته‌های مهم**
- دکمه تم باید در موبایل و دسکتاپ قابل دسترسی باشه
- استایل‌های تم روشن و سیاه باید در یک فایل CSS جداگانه باشه.
- ساختار فایل‌ها:
```
src/
├── core/
│   └── themeManager.js    # ماژول مدیریت تم (جدید)
├── styles/
│   ├── themes/
│   │   ├── dark.css       # تم تاریک (فعلی را به اینجا منتقل کن)
│   │   └── light.css      # تم روشن (جدید)
│   └── variables.css      # متغیرهای مشترک
```
- نحوه اعمال تم: تم‌ها از طریق یک attribute روی `<html>` کنترل بشن (مثلاً `data-theme="light"` یا `data-theme="dark"`)
  - CSS ها بر اساس این attribute استایل اعمال کنن
  - این روش به توسعه‌دهندگان اجازه می‌ده تم‌های سفارشی بسازن
- تمام رنگ‌ها و استایل‌ها از CSS Variables استفاده کنن
- قابلیت توسعه: ماژول `themeManager` طوری طراحی بشه که بعداً بشه تم‌های بیشتری (مثلاً `sepia`, `high-contrast`) اضافه کرد
  - کاربران بتونن تم‌های سفارشی خودشون رو با CSS اضافه کنن
- سندهای لازم رو هم بنویس (مثلا لیست کامل متغیرهای CSS رو در یک فایل جدا مستند کن تا کاربران بدونن چی رو می‌تونن تغییر بدن)

## پرامپت ۲۸
می‌خوام یک **سیستم مدیریت افزونه (Plugin System)** حرفه‌ای برای پروژه پیاده‌سازی کنی. هدف اینه که کاربران و توسعه‌دهندگان بتونن به راحتی و بدون دستکاری کدهای اصلی شیرازه، قابلیت‌های جدیدی به اون اضافه کنن.

1.  **فعال‌سازی از طریق `config.js`:** کاربر باید بتونه افزونه‌ها رو فقط با مشخص کردن **مسیر فایل** اون‌ها در آرایه `plugins` در فایل `config/config.js` فعال کنه. به این صورت:
```javascript
// config.js
plugins: [
'plugins/toc.js',
'plugins/search.js'
 ]
```
2. **بارگذاری هوشمند و پویا:** شیرازه باید این مسیرها رو بخونه و فایل‌های جاوااسکریپت افزونه رو به صورت **پویا (dynamic import)** لود کنه. اگه یک فایل پیدا نشد، برنامه نباید خراب بشه و فقط باید یک پیام خطا در کنسول نمایش بده.
3. **مدیریت CSS افزونه:** **(نکته مهم)** هر افزونه باید بتونه فایل CSS خودش رو هم به صفحه اضافه کنه. بهترین روش اینه که خود افزونه در زمان اجرا، یک تگ `<link>` برای فایل CSS مربوط به خودش بسازه و اون رو به `<head>` صفحه اضافه کنه.
4.  **یک افزونه نمونه خیلی ساده بساز:** برای اینکه بتونیم این سیستم رو تست کنیم، یک افزونه نمونه خیلی ساده به نام `hello.js` در پوشه `src/plugins/` بساز که فقط وقتی صفحه لود می‌شه، یک پیام "Hello from Plugin!" در کنسول چاپ کنه.

**یادآوری:**
هیچ تغییری در `index.html` نباید لازم باشه.

## پرامپت ۲۹
در کنسول خطای زیر نمایش داده می‌شه:

```js
Failed to load plugin: src/plugins/hello.js TypeError: error loading dynamically imported module: http://127.0.0.1:8000/src/core/src/plugins/hello.js app.js:134:25
    _loadPlugins http://127.0.0.1:8000/src/core/app.js:134
Loading module from “http://127.0.0.1:8000/src/core/src/plugins/hello.js” was blocked because of a disallowed MIME type (“text/html”).
```

## پرامپت ۳۰
افزونه‌ها الان فقط در زمان بارگذاری اجرا می‌شن و هیچ راهی برای تعامل با رویدادهای برنامه (مثل لود شدن یک صفحه جدید) ندارن.

1.  **ساخت `pluginManager.js`:**
   -   لطفاً یک ماژول جدید به نام `pluginManager.js` در `src/core/` بساز.
   -   منطق بارگذاری افزونه‌ها (`_loadPlugins`) رو از `app.js` به این ماژول جدید منتقل کن.
   -   این ماژول باید یک متد `notify(eventName, data)` داشته باشه تا بتونه رویدادها رو به همه افزونه‌های فعال ارسال کنه.

2.  **تعریف یک ساختار استاندارد برای افزونه‌ها:**
   -   هر افزونه باید یک کلاس باشه که متدهای زیر رو داشته باشه (البته همه اختیاری هستن):
      -   `onInit(app)`: وقتی افزونه برای اولین بار لود می‌شه، اجرا می‌شه.
      -   `onPageLoad(contentElement)`: هر بار که یک صفحه جدید لود می‌شه، اجرا می‌شه.
      -   `onDestroy()`: (برای آینده) وقتی برنامه بسته می‌شه.
   -   `pluginManager` باید بعد از لود کردن هر افزونه، `onInit` رو فراخوانی کنه.
   -   `app.js` باید بعد از لود شدن هر صفحه، از طریق `pluginManager`، رویداد `onPageLoad` رو به همه افزونه‌ها اطلاع بده.

3.  **قابلیت اضافه کردن CSS توسط افزونه:**
   -   لطفاً یک تابع کمکی (مثلاً `injectCSS`) در `pluginManager` یا یک فایل `utils` بساز که به افزونه‌ها اجازه بده فایل CSS خودشون رو به `<head>` صفحه اضافه کنن.

4.  **بازنویسی افزونه نمونه `hello.js`:**
   -   افزونه `hello.js` رو بر اساس این ساختار جدید بازنویسی کن:
      -   باید یک کلاس `HelloPlugin` رو `export default` کنه.
      -   در متد `onInit`، یک پیام در کنسول چاپ کنه و فایل `hello.css` خودش رو هم لود کنه.
      -   در متد `onPageLoad`، یک پیام دیگه در کنسول چاپ کنه تا مطمئن بشیم سیستم رویداد کار می‌کنه.
   -   یک فایل `hello.css` هم براش بساز که یک استایل ساده (مثلاً تغییر رنگ `body`) داشته باشه.

## پرامپت ۳۱
خطای زیر در کنسول نمایش داده می‌شه:

```js
GET
http://127.0.0.1:8000/core/pluginManager.js
NS_ERROR_CORRUPTED_CONTENT
Loading module from “http://127.0.0.1:8000/core/pluginManager.js” was blocked because of a disallowed MIME type (“text/html”).
127.0.0.1:8000
Failed to load plugin: src/plugins/hello.js TypeError: error loading dynamically imported module: http://127.0.0.1:8000/src/plugins/hello.js
```

## پرامپت ۳۱
برای سیستم افزونه نویسی یک سند کامل و جامع بنویس.

## پرامپت ۳۲
می‌خوام نخستین افزونه واقعی و کاربردی که **فهرست مطالب (Table of Contents)** هوشمنده را بسازی.

لطفاً افزونه `hello.js` و `hello.css` رو حذف کن و به جای اون‌ها، افزونه جدید `toc.js` و `toc.css` رو در پوشه `src/plugins/` بساز.
1.  **ظاهر و مکان:**
    -   فهرست مطالب باید در سمت **چپ صفحه** (مقابل منوی کناری) و با یک فاصله مناسب از بالای صفحه نمایش داده بشه.
    -   ظاهرش باید تمیز و با تم روشن و تاریک شیرازه هماهنگ باشه.
2.  **محتوا و ساختار:**
    -   باید به صورت خودکار تیترهای (`h1`, `h2`, `h3` و...) صفحه رو تشخیص بده و یک منوی قابل کلیک بسازه.
    -   باید از **ساختار تودرتو** پشتیبانی کنه (مثلاً `h3` زیرمجموعه `h2` باشه).
    -   اگه یک تیتر طولانی بود، باید به درستی با `...` کوتاه بشه.
3.  **عملکرد و تجربه کاربری:**
    -   وقتی کاربر روی یک آیتم کلیک می‌کنه، صفحه باید با یک **انیمیشن اسکرول نرم** به اون بخش پرش کنه.
    -   **Scrollspy:** وقتی کاربر صفحه رو اسکرول می‌کنه، آیتم مربوط به اون بخش در فهرست مطالب باید به صورت خودکار **هایلایت** بشه.
4.  **رفتار هوشمند:**
    -   اگه یک صفحه هیچ تیتری (یا تعداد کمی تیتر) نداشت، فهرست مطالب اصلاً نباید نمایش داده بشه.
    -   در صفحات کوچک (موبایل)، فهرست مطالب باید به طور خودکار مخفی بشه.
5.  **قابلیت پیکربندی در `config.js`:**
    -   کاربر باید بتونه افزونه رو به راحتی در `config.js` فعال و تنظیم کنه. لطفاً بخش تنظیمات `toc` رو در `config.js` به این صورت در نظر بگیر:
        ```javascript
        // در config.js
        toc: {
          enabled: true,       // فعال یا غیرفعال کردن
          maxDepth: 3,         // حداکثر عمق تیترها (مثلاً تا h3)
          title: 'در این صفحه', // عنوانی که بالای فهرست مطالب نمایش داده می‌شه
        }
        ```
    -   فراموش نکن که کاربر باید مسیر `src/plugins/toc.js` رو هم در آرایه `plugins` اضافه کنه.

## پرامپت ۳۳
بخش فهرست مطالب نمایش داده می‌شه اما خالیه. عکس مشکل را پیوست کردم.

## پرامپت ۳۴
قابلیت جمع‌شوندگی زیرعنوان‌ها را اضافه نکردی. از آیکون '›' برای جمع‌شوندگی استفاده کن، این آیکون باید سمت راست عنوان والد قرار بگیره.

## پرامپت ۳۵
تغییراتی که دادی مشکل‌های زیر را داره:
1. زیرعنوان‌ها باید یک تورفتگی کوچکی داشته باشن
2. به صورت پیش‌فرض همه عنوان و زیرعنوان‌ها نمایش داده بشه. (باز باشه)
3. زمانی که زیر عنوان نمایش داده می‌شه آیکون نمایش داده شده باید رو به پایین باشه. الان رو به بالاست که اشتباهه
4. وقتی متن در یک عنوان قرار داره اون عنوان در فهرست مطالب هایلایت نمی‌شه.
5. می‌خوام پس‌زمینه فهرست مطالب حذف بشه و فقط با یک خط عمودی سمت راست از متن جدا بشه

## پرامپت ۳۶
مشکل‌های زیر را برای فهرست مطالب بر طرف کن:
1. متاسفانه فهرست مطالب در صفحه‌های کوچک مانند موبایل نمایش داده می‌شه که درست نیست و نباید نمایش داده بشه.
2. با اضافه شدن فهرست مطالب، محتوای صفحه فاصله یکسانی با پنل کناری و فهرست مطالب نداره. فاصله از پنل کناری زیاده و از فهرست مطالب فاصله کمی داره، من عکس مشکل را بارگذاری می‌کنم.

## پرامپت ۳۷
خب حالا ازت می‌خوام برای این افزونه‌ای که نوشته در پوشه سندها در پوشه‌ای با نام `plugins` یک سند کامل بنویسی.

## پرامپت ۳۸
می‌خوام معماری شیرازه رو طوری بازسازی کنی که کاربر بتونه به راحتی از طریق فایل پیکربندی، مفسر مارک‌دان مورد نظرش رو انتخاب کنه. حتی باید بتونه مفسر مارک‌داون سفارشی خودش را اضافه کنه.

برای پیاده‌سازی حتما به موارد زیر دقت کن:
1. معماری ماژولار پروژه را حفظ کن.
2. کاربر باید بتونه بدون تغییر در هسته شیرازه و به راحتی مفسر خودش را اضافه کنه.
3. کاربر مفسر خودش را در پوشه `config/parser` قرار بده.

## پرامپت ۳۹
من برای بررسی افزودن مفسر مارک‌داون سفارشی کارهای زیر را انجام دادم:
1.  یک فایل جدید به نام `myParser.js` در پوشه `config/parsers/` ساختم.
2.  کد زیر رو داخلش کپی کردم:
    ```javascript
    // config/parsers/myParser.js
    export default class MyParser {
      parse(text) {
        return `<h1>این یک مفسر سفارشی است!</h1><p>${text}</p>`;
      }
    }
    ```
3.  در `config.js`، بخش `markdown` رو به این صورت تغییر دادم:
    ```javascript
    markdown: {
      parser: {
        path: 'config/parsers/myParser.js'
      }
    },
    ```
4.  صفحه رو رفرش کردم.
5.  **نتیجه مورد انتظار:** تمام صفحه‌ها باید با تیتر "این یک مفسر سفارشی است!" نمایش داده بشن.

اما در کنسول خطای زیر نمایش داده می‌شه:

```js
Failed to initialize markdown parser: Error: Invalid parser configuration: {"0":"m","1":"a","2":"r","3":"k","4":"e","5":"d"}
    init http://127.0.0.1:8000/src/core/parserManager.js:35
    start http://127.0.0.1:8000/src/core/app.js:38
    main http://127.0.0.1:8000/src/index.js:14
parserManager.js:46:21
Shirazeh encountered an error: Markdown parser could not be loaded. Please check your 'markdown.parser' configuration. Invalid parser configuration: {"0":"m","1":"a","2":"r","3":"k","4":"e","5":"d"}
```

## پرامپت ۴۰
من می‌خوام مفسر پیش‌فرض شیرازه به «پارس‌نشان» که یک مفسر سفارشی‌سازی شده براساس `markdown-it` است جایگزین بشه.
1. من فایل‌های `parsneshan.js` و `parsneshan.css` بارگذاری کردم.
2. دقت کن که احتمال زیاد استایل‌های این کتابخانه با استایل‌های پروژه همپوشانی داشته باشه، مراقب باشه استایل تکراری در کد درج نکنی.
3. این مفسر سفارشی به کتابخانه `markdown-it` نیاز داره
4. از اونجایی که `markdown-it` قابلیت اضافه شدن افزونه داره، در فایل پیکربندی اجازه بده کاربر افزونه اضافه کنه.

دقت کن که با افزودن پارس‌نشان هیچ بخشی از پروژه از کار نیافته.

## پرامپت ۴۱
در کنسول خطای زیر نمایش داده می‌شه:
```js    
Shirazeh encountered an error: can't access property "push", this.tokens is null
```

## پرامپت ۴۰
تابع `deepMerge` در فایل `src/core/configManager.js` دوباره به نسخه باگ‌دار قبلی برگشته. این نسخه نمی‌تونه یک مقدار رشته‌ای (مثل `'parsneshan'`) رو با یک شیء (مثل `{ path: '...' }`) جایگزین کنه.

**درخواست:**
لطفاً تابع `deepMerge` رو با نسخه صحیح و اصلاح شده زیر جایگزین کن:

```javascript
function deepMerge(target, source) {
    const output = { ...target };
    if (isObject(target) && isObject(source)) {
        Object.keys(source).forEach(key => {
            const sourceValue = source[key];
            const targetValue = target[key];

            // اگر هر دو مقدار شیء باشن (و آرایه نباشن)، به صورت بازگشتی ادغام کن
            if (isObject(sourceValue) && isObject(targetValue) && !Array.isArray(sourceValue)) {
                output[key] = deepMerge(targetValue, sourceValue);
            } else {
                // در غیر این صورت (اگر نوع‌ها متفاوت باشن یا یکی از اونها شیء نباشه)،
                // مقدار جدید (source) رو جایگزین قبلی کن.
                output[key] = sourceValue;
            }
        });
    }
    return output;
}
```
و مطمئن شو که تابع `isObject` هم به درستی آرایه‌ها رو چک می‌کنه:
```javascript
function isObject(item) {
    return (item && typeof item === 'object' && !Array.isArray(item));
}
```

لطفاً فقط فایل `configManager.js` رو با این دو تابع اصلاح شده بهم بده.

## پرامپت ۴۱
اگر در یک فایل مارک‌داون نشانه‌گذاری تصویر باشه، تصویر به درستی نمایش داده نمی‌شه و از محدوه مشخص شده برای محتوا بیرون می‌زنه.

نمونه نشانه‌گذاری: ![](/assets/icon/shirazehLogo.png)

عکس مشکل را پیوست کردم.

## پرامپت ۴۲

می‌خوام شیرازه بتونه فایل‌های مارک‌دان رو مستقیماً از یک **URL خارجی** (مثل یک فایل در گیت‌هاب) بخونه و نمایش بده. کاربر باید بتونه در منوی کناری (`sidebar.md`) به یک فایل مارک‌دان که روی اینترنت قرار داره لینک بده و شیرازه اون رو مثل یک صفحه معمولی نمایش بده.

**مثال لینک خارجی:** `https://raw.githubusercontent.com/barnevis/parsiNegar/refs/heads/main/docs/markdownGuide.md`

- دقت کن که معماری ماژولار را حفظ کنی
- پیکربندی نوار کناری مثل قبل کار کنه، این ویژگی جدید به پیکربندی قبلی اضافه بشه
- در فایل پیکربندی یک گزینه برای فعال‌سازی این مورد قرار بده
- برای جلوگیری از خطای CORS کاربر امکان معرفی آدرس corsProxy خودش را داشته باشه.
- بازنویسی پیوندها/تصاویر داخل محتوای ریموت: لینک‌های نسبی داخل فایل ریموت باید نسبت به URL همان فایل حل شوند. همچنین لینک‌های نسبی را به روتر خودمان تبدیل کنیم تا بتوانیم بین فایل‌های ریموت هم جابه‌جا شویم.

## پرامپت ۴۳
زمانی که در فایل پیکربندی بخش ریموت را غیرفعال می‌کنم، هنوز گرفتن داده از ریموت کار می‌کنه.

```js
remote: {
  enabled: false,
  corsProxyUrl: '',
}
```

## پرامپت ۴۴
خب ازت می‌خوام با توجه به تغییراتی که اعمال کردی سندها را کامل کنی.

## پرامپت ۴۵
جهت متن بلوک کد چپ به راست بشه. دقت کن که کد خطی راست به چپ باقی بمونه.

## پرامپت ۴۶
با تغییری که ایجاد کردی اگر در یک خط محتوا نشانه‌گذاری به صورت زیر باشه از سمت چپ به راست نمایش داده می‌شه در حالیکه باید راست به چپ باشه:

نمونه یک : ### `files`

نمونه دو: -   `sidebar`: فایل مارک‌داونی که ساختار منوی کناری را تعریف می‌کند.

## پرامپت ۴۷
می‌خواهم شیرازه به‌صورت پیش‌فرض با فونت «وزیرمتن» کار کنه (کاملاً آفلاین) و در عین حال کاربران بتونن بدون دست‌زدن به کدهای هسته، فونت‌های دلخواه خودشون را اضافه و فعال کنند.

- حالت پیش‌فرض:
  - شیرازه بدون نیاز به اینترنت و بدون هیچ پیکربندی اضافه‌ای با فونت «وزیرمتن» نمایش داده بشه.
- حالت سفارشی توسط کاربر:
  - کاربر می‌تونه در پروژه خود پوشه‌ای به نام `config/font` بسازه، فایل‌های فونت و یک فایل معرفی فونت (CSS) را اونجا قرار بده.
  - کاربر با یک تنظیم ساده در فایل پیکربندی (config) می‌تونه اعلام کنه که از فونت سفارشی استفاده بشه.
  - پس از اعمال پیکربندی، کل رابط (متن‌ها) با فونت سفارشی نمایش داده بشه.
  - برای فونت کد (code) هم امکان تعریف فونت جداگانه وجود داشته باشه.

محدودیت‌ها و اصول
- همه‌چیز آفلاین باشه؛ هیچ وابستگی به CDN یا اینترنت نداشته باشیم.
- کاربر نباید index.html یا فایل‌های هسته را ویرایش کنه.
- معماری پروژه ماژولار باقی بمونه؛ این قابلیت در قالب یک بخش پیکربندی/مدیریت فونت به هسته اضافه بشه.
- از هم‌پوشانی و تکرار استایل‌ها جلوگیری بشه و با تم‌های روشن/تاریک فعلی تداخلی نداشته باشه.
- تغییر فونت باید روی تمام متن‌های اصلی اعمال بشه و فونت مخصوص کدها هم جدا قابل تنظیم باشه.
- اندازه متن و فاصله خطوط به شکل ساده قابل تنظیم باشه (مثلاً عددی برای اندازه پایه و ضریب برای فاصله خطوط).

گزینه‌های پیکربندی (برای کاربر، به زبان ساده)
- استفاده از فونت پیش‌فرض (وزیرمتن) یا فونت سفارشی.
- اگر کاربر فونت سفارشی می‌خواد:
  - در پوشه config/font فایل‌های فونت و یک فایل معرفی (CSS) قرار می‌ده.
  - در پیکربندی، مسیر همین فایل معرفی را اعلام می‌کنه.
  - در پیکربندی می‌تونه نام خانواده فونت متن و نام خانواده فونت کد را مشخص کنه.
- امکان تنظیم اندازه پایه متن (مثلاً 16) و فاصله خطوط (مثلاً 1.8).

رفتار در صورت خطا یا نبودن فایل‌ها
- اگر کاربر مسیر نادرست بده یا فایل فونت/معرفی پیدا نشد:
  - برنامه نباید خراب بشه؛ به صورت خودکار به فونت پیش‌فرض (وزیرمتن) برگرده.
  - یک پیام خطای قابل‌پیگیری در کنسول ثبت بشه (نیازی به نمایش به کاربر نهایی نیست).
- اگر کاربر هیچ تنظیمی انجام نداد، همان پیش‌فرض کار کنه.

## پرامپت ۴۸
در کنسول مرورگر خطای زیر نمایش داده می‌شه:

```js
downloadable font: font load failed (font-family: "Vazirmatn" style:normal weight:100..900 stretch:100 src index:0): status=2152398858 source: data:font/woff2;base64,d09GMgABAAAAAAVwAA4AAAAAC+gAAAUpAAEAAAAAAAAAAAAAAAAAAAAAAAAAAAAABk4ALAoUNAE2AiQDBgsGAAQgBYt0BzgebhCB1L4j8 … v/s/8H/gf/D/8v/s/+r/6P/p/+j/wf+D/yP/e/8n/u/+T/wf/L/7P/Y/+T/wP/Q/7n/e/+L/2v/g/8z/w//T/+j/5P/s/+z/2v+p/8H/2f+z/6n/qf+5/9r/sv/e/wA= 
```

و اینکه فایل `markdownParser.js` هیچ کدی داخلش نیست.

## پرامپت ۴۹
برای اینکه فونت پیش‌فرض به صورت آفلاین کار کنه من فایل‌های مربوط به فونت وزیر متن را در پوشه `src/lib/vendor/fonts/vazirmatn/` قرار دادم.

`Vazirmatn-Bold.woff2  Vazirmatn-Medium.woff2  Vazirmatn-Regular.woff2`

ازت می‌خوام کد را اصلاح کنی تا از این فونت‌ها استفاده کنه.